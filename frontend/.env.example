# Point the app at the GradeFlow API to use real accounts and server-side grading.
# Leave this commented out to run the frontend on its own: accounts and the
# workspace are then stored in your browser and no server is needed.
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
